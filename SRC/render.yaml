services:
  - type: web
    name: gongju-backend
    env: python
    buildCommand: pip install -r SRC/requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: free
    build:
      context: SRC
