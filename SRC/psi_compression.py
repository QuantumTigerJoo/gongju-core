# psi_compression.py

def compress_thought(thought):
    """
    Compresses a user's thought into a more concise, essence-like form.
    For now, this is a placeholder that simply summarizes the input.
    """
    if not thought.strip():
        return "That thought is empty. Try sharing a fuller idea to compress."

    # Placeholder compression logic (for MVP): just a poetic echo
    return f"🧩 Compressed Thought: {thought.strip()[:10]}... (essence captured)"
