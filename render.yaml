services:
  - type: web
    name: gongju-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn SRC.app:app --host 0.0.0.0 --port 10000
    plan: free
    build:
      context: .
